# Backend URL for chatbot - used by frontend
# For local development: http://localhost:5000/api/chat
# For Lambda production: https://your-lambda-url.lambda-url.us-east-1.on.aws
BACKEND_URL=http://localhost:5000/api/chat

# Note: OPENROUTER_API_KEY should be set in Lambda environment variables (not here)
# The Lambda backend handles the API key securely








